<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/cor_branca"/>
    <!-- Removido o corners -->
    <!-- Se quiser sem borda nenhuma, remova o stroke abaixo -->
    <stroke android:width="1dp" android:color="@color/cor_primaria"/>
</shape>